# Chat with Files - Frontend

React frontend for the Chat with Files application.

## Features

- 📄 File upload (PDF, TXT, DOCX)
- 💬 Chat interface with document
- 📑 PDF generation from chat responses
- 🎨 Modern, responsive UI

## Setup

1. Install dependencies:
```bash
npm install
```

2. Start development server:
```bash
npm run dev
```

The app will run on `http://localhost:5173`

## Build for Production

```bash
npm run build
```

## Backend Integration

Make sure the backend is running on `http://127.0.0.1:8000`

The frontend is configured to connect to the backend API automatically.
